# Git secrets patterns - DO NOT COMMIT THESE PATTERNS
# This file helps identify sensitive data before it gets committed

# Firebase API Keys
AIzaSy[A-Za-z0-9_-]{33}

# Generic API Key patterns
api[_-]?key["\s]*[:=]["\s]*[A-Za-z0-9_-]{20,}
apiKey["\s]*[:=]["\s]*[A-Za-z0-9_-]{20,}

# Password patterns
password["\s]*[:=]["\s]*[^"\s]{8,}
passwd["\s]*[:=]["\s]*[^"\s]{8,}

# FTP/Database credentials
host["\s]*[:=]["\s]*[^"\s]+\.(com|net|org)
user["\s]*[:=]["\s]*[^"\s]{4,}

# Email service keys (SendGrid, EmailJS, etc.)
SG\.[A-Za-z0-9_-]{22}\.[A-Za-z0-9_-]{43}

# AWS Keys
AKIA[0-9A-Z]{16}

# Private keys
-----BEGIN.*PRIVATE KEY-----
